# AI Agent team instructions

**Mission:** Build the "Lumicello" High-Conversion Static Site.
**Deployment Target:** Render.com (Static Site).
**Version Control:** Git (Already initialized).
**Starting Context:** The agents are deployed into a directory containing `assets/` (raw PNGs) and `design_specs/` (Markdown requirements).

-----

### **1. Environment & Workflow Orchestration**

**Global Directive:** All agents must work in parallel. Use `git` to manage merges.
**Repository State:** *Existing*. Do not overwrite `assets/*.png` or `design_specs/*.md`.

| **Track** | **Agent Role** | **Primary Responsibility** | **Input Source** |
| :--- | :--- | :--- | :--- |
| **Track A** | `ARCHITECT_BOT` | Git Init, File Structure, Core HTML/CSS, Render Config. | `design_specs/` |
| **Track B** | `ASSET_OPT_BOT` | Optimize existing PNGs to WebP, Generate Social Icons. | `assets/` |
| **Track C** | `MOTION_BOT` | Implement "Fluid" JS logic & Scroll Observers. | `website_specs` |
| **Track D** | `GROWTH_BOT` | Kit.com Integration, Social Sticky Bar, Copy Injection. | `company_context.md` |

-----

### **2. Agent-Specific Instructions**

#### **🤖 AGENT A: `ARCHITECT_BOT` (Core Structure & Deployment)**

**Objective:** Establish the codebase and ensure it is Render-ready.

  * **Step 1 (Git & Init):**
      * Run `git init`.
      * Create a `.gitignore` (ignore `node_modules`, `.DS_Store`).
  * **Step 2 (Directory Hygiene):**
      * Move the *existing* `assets/` folder into a new clean root structure.
      * **Structure:**
        ```text
        / (Root)
        ├── index.html       <-- Entry point for Render
        ├── css/
        │   ├── style.css
        │   └── variables.css
        ├── js/
        │   └── main.js
        └── assets/          <-- Move existing PNGs here
            ├── images/      <-- Place hero.png, etc. here
            └── icons/       <-- For SVGs
        ```
  * **Step 3 (The "Warm" Layout):**
      * Read `design_specs/design_specs_requirements.md`.
      * Write `index.html` referencing the *future* WebP images (e.g., `<img src="assets/images/hero.webp">`).
      * **Crucial:** Render.com looks for `index.html` at the build root. Ensure it is not nested in a `public` or `dist` folder for this simple static setup.

#### **🤖 AGENT B: `ASSET_OPT_BOT` (Optimization & Missing Assets)**

**Objective:** Polish existing assets and generate missing UI elements.

  * **Task 1 (Optimize Existing):**
      * Take `hero.png`, `exploration_kit.png`, and `curiosity_fingerprint.png` from the source.
      * Convert them to **.webp** (Quality: 85%) for performance.
      * *Design Tweet:* If `hero.png` has sharp edges, apply a CSS `mask-image` or feathering in the conversion process to match the "Soft/No Sharp Edges" requirement.
  * **Task 2 (Generate Missing Icons):**
      * The source folder is missing social icons. Generate/Fetch **monochrome** SVG icons for:
          * **Line** (Round chat bubble style).
          * **Facebook** (Round style).
          * **WhatsApp** (Round style).
      * *Style:* Color them `#1A2B4C` (Jefferson Blue) to match the brand.
  * **Task 3 (Favicon):**
      * Create a `favicon.ico` based on the `curiosity_fingerprint.png`.

#### **🤖 AGENT C: `MOTION_BOT` (Fluidity & Feel)**

**Objective:** Implement the "Comfortable Browsing" vibe.

  * **Task 1 (Scroll Observer):**
      * Create `js/main.js`.
      * Target the `.kit-card` (Exploration Kits) and `.hero-content` classes.
      * Apply the `cubic-bezier(0.2, 0.8, 0.2, 1)` easing defined in specs.
  * **Task 2 (Data Viz Animation):**
      * Target the `curiosity_fingerprint.webp` image.
      * Add a subtle CSS rotation or pulse: `@keyframes float { 0% { transform: translateY(0px); } 50% { transform: translateY(-10px); } 100% { transform: translateY(0px); } }`.

#### **🤖 AGENT D: `GROWTH_BOT` (Conversion Layer)**

**Objective:** Monetization & Lead Capture.

  * **Task 1 (Kit.com Form):**
      * Inject the newsletter signup form into the `index.html` generated by Architect Bot.
      * **Constraint:** Do not use the default Kit.com styling. Force the input fields to use `var(--radius-btn)` from `variables.css`.
  * **Task 2 (Social Sticky Bar):**
      * Create a `<aside class="social-sticky">` element.
      * Populate it with the SVGs created by Asset Bot.
      * *Linkage:* Ensure these open in a new tab (`target="_blank"`).
  * **Task 3 (Copy Injection):**
      * Read `design_specs/company_context.md`.
      * Extract the "Emotional Impact" keywords.
      * Ensure the H1 tag reads: *"Unlocking the Genius in Every Child"* (or the latest variant from the specs).

-----

### **3. Deployment Instructions (Render.com)**

*Once agents complete their tasks and merge via Git:*

1.  **Repo Setup:** Commit all changes to `main`.
2.  **Render Config:**
      * Create a New **Static Site**.
      * **Build Command:** (Leave empty, we are pure HTML/CSS).
      * **Publish Directory:** `./` (Root directory).
3.  **Domain:** Link to `lumicello.com` (or staging URL).

### **4. Final Execution Trigger**

> "Team, assume `assets/` and `design_specs/` are present in the root. Initialize the repository, optimize the provided images, and build the static structure immediately. Prioritize the 'Soft & Fluid' aesthetic."
